# 한 학기 데이터 분석 및 시각화 프로젝트 가이드

> **핵심 안내**  
> 한 학기 동안 무엇을 할지 정하고 계획을 짜서 발표하면 됩니다. (계획은 진행 과정에서 계속 업데이트/변경될 수 있습니다.)  
> **이미 아는 결론을 예쁘게 다시 그리는 것은 이 수업의 목적이 아닙니다.**

---

## 1. 첫 장의 한 문장 선언 (슬라이드 첫 장에 그대로 삽입)

> **"나는 `[        ]` 데이터로, `[        ]`를 보여주려 한다.**  
> **이 그림을 보기 전에 내가 몰랐던 것은 `[        ]`이다. `[        ]` 분석 혹은 `[        ]` 그래프를 통해 `[        ]`를 발견할 수 있다."**

---

## 2. 세부 계획 (4대 필수 항목)

아래 4개 항목은 **서로 필연적인 인과관계**로 연결되어 있어야 합니다.  
*맥락 때문에 그 방법이 필요하고, 그 방법의 출력 형태 때문에 그 그림이어야 하며, 그래서 그 스택이어야 합니다.* (하나를 제외해도 나머지가 성립한다면 아직 긴밀하게 연결되지 않은 것입니다.)

### (1) 데이터의 맥락
- **내용**: 이 데이터가 어디서, 왜, 누가, 어떤 조건에서 만들어졌는가? 혹은 데이터를 어떻게 만들 예정인가? 어떤 공공 데이터를 가져올 것이며 어떤 인자(feature/variable)들을 포함하는가?
- **예시**:
  > *"A동 BEMS 15분 단위 전력. 2022년 설치, 요금 검침이 목적이라 계측 위치가 층별이 아니라 인입 단위. 방학 중 2주 결측. 재실 인원은 측정되지 않음 — 그래서 부하 변동을 날씨로만 설명해야 하는 한계가 있음."*

### (2) 분석 방법
- **내용**: 데이터에서 메시지를 끌어내기 위해 무엇을 계산할 것인가?
- **핵심 질문**: *"왜 이 방법이어야 하는가?"*  
  (기법이 화려해서가 아니라, 질문 해결을 위해 필연적으로 그 테크닉이 요구되어야 함)
- **예시 기법**:
  - 상관·회귀분석
  - Change-point 회귀 (난방·냉방 전이점 추정)
  - 클러스터링 (일별 부하 프로파일 유형 분류)
  - 스펙트럼 / 웨이블릿 (주기 성분 분해)
  - Gaussian Process (불확실성을 동반한 보간)
  - 계층 베이지안 (건물군 비교) 등

### (3) 시각화 형태
- **내용**: 분석 결과를 어떤 형태의 그림/시각화로 구현할 것인가? 그 시각화 형태가 메시지의 어느 부분을 전달하는가?
- **예시 형태**:
  - Carpet plot (시간축 2차원 전개로 일간·계절 패턴 동시 표현)
  - Energy signature 산점도 + 전이점
  - 불확실성 밴드가 포함된 시계열 플랏
  - Small multiples (건물별 비교)
  - Sankey 다이어그램 (에너지 흐름)
  - 임베딩 산점도 + 프로파일 미리보기

### (4) 플랏의 기술 스택
- **내용**: 무엇으로 만들 것인가? 현재 다룰 수 있는 도구와 앞으로 배워야 할 도구를 명확히 구분하여 작성.
- **예시**:
  > *"Python + pandas(가능), matplotlib(가능), Plotly / D3.js로 인터랙티브(배워야 함), 최종은 HTML 단일 파일로 배포"*

---

## 3. 필수 그림 2장

계획 및 발표에는 반드시 다음 2장의 그림이 포함되어야 합니다.

| 구분 | 설명 |
| :--- | :--- |
| **Baseline** | 데이터를 가장 평범하고 일반적으로 그린 것 |
| **Attempt** | 나아가려는 방향의 초기 시도 (거칠어도 무방하며, 손그림 + 부분 구현도 인정) |

> **비교 및 도출 메시지**:  
> 두 그림을 대조하며 **"Baseline에서는 안 보이던 것이 Attempt에서는 이렇게 보인다"**와 같은 예상 결론 및 인사이트를 제시해야 합니다.

---

## 4. 데이터 수집 기준

- **원칙**: 스스로 직접 구함
- **데이터 소스**:
  - 공개 데이터셋
  - 본인 연구 데이터 / 시뮬레이션 데이터
  - 합성(Synthetic) 데이터 (※ 합성 데이터인 경우 어떤 구조/패턴을 심었는지 생성 과정을 명시할 것)

---

## 5. 환영 및 평가 포인트

- **인터랙티브 시각화 (Plotly, D3 등)**:
  - 환영하지만, 반드시 *"Hover나 Zoom이 내 메시지에 왜 필요한가?"*를 한 줄로 설명해야 합니다.
  - 정적 그림으로 충분한데 불필요하게 붙인 인터랙션은 가점 요소가 아닙니다.
- **고급 분석 기법 (Bayesian, Gaussian Process, 신호처리, ML 등)**:
  - 해당 방법이어야만 도출되는 시각화 형태라면 적극 환영합니다.
  - **'새롭다'의 기준은 최신 기술 여부가 아니라, 'Baseline에서 보이지 않던 인사이트가 드러나는가'입니다.**
  - **산점도 하나만으로도 핵심 메시지를 명확히 증명해낸다면 그것이 더 높은 평가를 받습니다.**

---

## 6. 학기말 최종 제출물

- **제출물 구성**:
  1. 누적 산출물 (`.pptx` 또는 `.html`)
  2. 전체 소스 코드
- **운영 방식**:
  - 이 슬라이드/문서 덱은 학기 내내 동일한 파일로 지속해서 누적 업데이트됩니다.
  - **시행착오 보존**: 중간에 실패하거나 폐기한 시도도 삭제하지 말고 **부록(Appendix)**으로 남겨둘 것.
  - 학기말 종합 발표는 완성된 단일 결과물만을 보여주는 것이 아니라, **한 학기 동안 탐색하고 발전해 온 사고와 분석의 궤적**을 보여주는 것입니다.
